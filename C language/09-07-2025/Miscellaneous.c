#include <stdio.h>

int main()
{
    int x;
    double y = 8.00000000;

    printf("\n size of int : %zu bytes", sizeof(int));
    printf("\n size of x : %zu bytes", sizeof(x));
    printf("\n size of double: %zu bytes", sizeof(double));
    printf("\n size of y : %zu bytes", sizeof(y));
    printf("\n size of y : %zu bytes", sizeof(y));

    return 0;
}